# Database Configuration
# Direct Database URL - untuk migrations & Prisma Studio
DIRECT_DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# Accelerate URL - untuk runtime queries (lebih cepat, di-cache)
# Atau gunakan DATABASE_URL langsung ke PostgreSQL jika tidak pakai Prisma Accelerate
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# Server Configuration
NODE_ENV=production
PORT=3000

# Optional: Next Auth (jika digunakan)
# NEXTAUTH_URL=https://your-domain.railway.app
# NEXTAUTH_SECRET=your-secret-key-here

# Optional: CORS Origin (jika diperlukan)
# CORS_ORIGIN=https://your-domain.railway.app
